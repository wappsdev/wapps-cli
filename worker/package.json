{
  "name": "secrets-gate",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "wapps-secrets secrets-gate Worker (G6 core) — verify-only crypto + per-project write serializer",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "gen-vectors": "cd .. && go run ./worker/testdata-gen/main.go"
  },
  "dependencies": {
    "@noble/curves": "^1.9.6",
    "@noble/hashes": "^1.8.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.9.0",
    "@cloudflare/workers-types": "^4.20250601.0",
    "typescript": "^5.7.3",
    "vitest": "~3.2.0",
    "wrangler": "^4.90.0"
  }
}
